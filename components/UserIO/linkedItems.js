import React from 'react';
import {Text, View, Button,} from 'react-native';

const  radioButton = (MyJson) => {
  return (
      <View>
        <Text>{MyJson.strDrink}</Text>
    </View>
  );
}
export default radioButton;
