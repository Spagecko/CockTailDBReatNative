import React from 'react';
import { Text } from 'react-native';

const  testPage = (props) => {
  return (
    <Text>Hello this a test page {props.Id} </Text>
  );
}
export default testPage;

